apiVersion: v1
kind: Service
metadata:
  name: iris-model-service
  namespace: mlops-k8s-deployment-dev
spec:
  type: LoadBalancer
  selector:
    app: iris-serving
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
